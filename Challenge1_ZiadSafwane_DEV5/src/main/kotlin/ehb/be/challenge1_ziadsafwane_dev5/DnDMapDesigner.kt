package ehb.be.challenge1_ziadsafwane_dev5

class DnDMapDesigner {
    fun placeAsset(asset: String, x: Int, y: Int, elevation: Int, direction: String) {
        // Logic to place asset based on position, direction, and elevation
        println("Placing asset $asset at ($x, $y), elevation $elevation, facing $direction")
    }

}